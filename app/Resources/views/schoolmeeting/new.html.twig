{% extends 'backAdmin.html.twig' %}

{% block content %}
    <h1>Schoolmeeting creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input class="btn btn-custon-four btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a  href="{{ path('schoolmeeting_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
