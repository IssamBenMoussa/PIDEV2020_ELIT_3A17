{% extends 'base.html.twig' %}

{% block content %}
    <br><br>  <h1>Application creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-custon-four btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('application_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
