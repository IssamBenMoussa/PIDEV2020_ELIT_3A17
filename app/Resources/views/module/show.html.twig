{% extends 'base.html.twig' %}

{% block content %}
    <h1>Module</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ module.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ module.nom }}</td>
            </tr>
            <tr>
                <th>Nombrecours</th>
                <td>{{ module.nombreCours }}</td>
            </tr>
            <tr>
                <th>Disponibilitemodule</th>
                <td>{{ module.disponibiliteModule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('module_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('module_edit', { 'id': module.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
