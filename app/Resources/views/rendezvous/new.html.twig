{% extends 'base.html.twig' %}

{% block hey %}
    <br><br><h1> Schedule your date </h1>
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    {{ form_start(form) }}

        {{ form_widget(form) }}
    <input class="btn btn-custon-four btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('rendez_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
