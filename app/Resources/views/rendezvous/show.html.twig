{% extends 'base.html.twig' %}

{% block hey %}
&nbsp  <section class="content"  >
    &nbsp <div class="row" align="center" >
        &nbsp  <div class="col-xs-12">
            &nbsp   <div class="box" style="width:1900px; border: 1px ">
                <br></br>
                <div class="box-header"><center>
                        <h3 class="box-title" > &nbsp &nbsp &nbsp  APPOINTEMENT INFORMATION </h3></center>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">
                    &nbsp &nbsp &nbsp &nbsp    &nbsp  &nbsp <table id="example2"  class="table table-bordered table-hover"  >
                        <thead>
                        <tr>

                            &nbsp &nbsp &nbsp   &nbsp  <tbody align="center">

                        <tr>
                            <tr>
                            <th>CIN</th>
                            <td>{{ rendezvous.cin }}</td>
                        </tr>
                <th>nom</th>
                <td>{{ rendezvous.nom }}</td>
            </tr>

            <tr>
                <th>prenom</th>
                <td>{{ rendezvous.prenom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if rendezvous.date %}{{ rendezvous.date|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>



        </tbody>
                    </table>
                        <a title="Print Screen" alt="Print Screen" onclick="window.print();" target="_blank" style="cursor:pointer;"  class="btn btn-success"> print your appointement  </a>

                    <div align="center">

                        <div style="width: 800px; height: 580px" id="mapContainer"></div>
                        <script>
                            // Initialize the platform object:
                            var platform = new H.service.Platform({
                                'apikey': 'X8OII43H8M56KCQV4NiEWbRfyCMPmJgCt76JakrYQtM'
                            });

                            // Obtain the default map types from the platform object
                            var maptypes = platform.createDefaultLayers();

                            // Instantiate (and display) a map object:
                            var map = new H.Map(
                                document.getElementById('mapContainer'),
                                maptypes.vector.normal.map,
                                {
                                    zoom: 15,
                                    center: { lng:10.1857, lat:36.9007}
                                });


                        </script>
                    </div>

{% endblock %}
