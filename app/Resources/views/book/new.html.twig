{% extends 'backAdmin.html.twig' %}

{% block content %}
    <h1>Book creation</h1>
    {% form_theme form 'bootstrap_4_layout.html.twig' %}

    {{ form_start(form) }}

    {{ form_widget(form) }}

        <input type="submit" value="Create" class="btn btn-info" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('book_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}
