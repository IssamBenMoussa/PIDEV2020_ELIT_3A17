<form method="get" action="{{ action }}" enctype="application/x-www-form-urlencoded">

    <select name="{{ filterFieldName }}" class="form-control">
        {% for field, label in fields %}
            <option value="{{ field }}"{% if selectedField == field %} selected="selected"{% endif %}>{{ label }}</option>
        {% endfor %}
    </select>

    <input type="text" value="{{ selectedValue }}" name="{{ filterValueName }}" class="form-control" />
<br>
    <button class="btn btn-success"><i class="fa fa-check edu-checked-pro" aria-hidden="true"></i> {{ options.button }}</button>

</form>
